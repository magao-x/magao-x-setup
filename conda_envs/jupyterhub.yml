channels:
  - conda-forge
dependencies:
  - jupyterhub
  - jupyterlab

  # if declaring pip dependencies, you also need to install
  # pip itself:
  - pip
  - pip:
    - ipywidgets
    - anywidget
    - jupyterlab-speedy-unfold
    - fitsview
    - pyviewarr
